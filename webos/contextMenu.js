(function(){const menu=()=>document.getElementById("context-menu");function hide(){menu().classList.add("hidden");menu().innerHTML=""}function show(x,y,items){const el=menu();el.innerHTML="";items.forEach(item=>{if(item==="divider"){const d=document.createElement("div");d.className="context-divider";el.appendChild(d);return}const b=document.createElement("button");b.className="context-item";b.innerHTML=`<span>${item.icon||""}</span><span>${item.label}</span>`;b.onclick=()=>{hide();item.action?.()};el.appendChild(b)});el.classList.remove("hidden");const r=el.getBoundingClientRect();el.style.left=Math.max(8,Math.min(x,innerWidth-r.width-8))+"px";el.style.top=Math.max(8,Math.min(y,innerHeight-r.height-8))+"px"}document.addEventListener("click",hide);document.addEventListener("keydown",e=>{if(e.key==="Escape")hide()});window.NovaContextMenu={show,hide}})();
